---
// src/pages/docs/react/full.astro
import Layout from '../../../layouts/Layout.astro'
---

<Layout title="Full API - React">
  <div
    class="mb-4 text-sm font-mono text-neutral-500 uppercase tracking-widest"
  >
    @elekcsv/react
  </div>

  <h1 class="mb-8 border-b-2 border-black pb-4">2.4 Full API Reference</h1>

  <p class="text-xl text-neutral-600 leading-relaxed mb-10">
    A complete look at the configuration arguments and the reactive return
    values provided by the <code>useCSVImporter</code> hook.
  </p>

  <h2>Configuration Arguments</h2>
  <p>
    The hook accepts exactly the same parsing and schema options as
    `@elekcsv/core`, plus React-specific lifecycle methods.
  </p>

  <pre><code>type ReactImporterConfig&lt;T&gt; = &#123;
  // Standard Core Configuration
  schema: SchemaConfig;
  options?: ParserOptions;

  // React Lifecycle Hooks
  onStart?: () =&gt; void;
  onBatch?: (batch: T[], context: BatchContext) =&gt; Promise&lt;void&gt; | void;
  onProgress?: (progress: number) =&gt; void;
  onComplete?: (data: T[], durationMs?: number) =&gt; void;
  onError?: (errors: ErrorMap[]) =&gt; void;
&#125;</code></pre>

  <h2>Hook Return Values</h2>
  <p>
    The destructuring output provides data accessors, state flags, and the
    trigger method.
  </p>

  <pre><code>type HookReturns&lt;T&gt; = &#123;
  // Data Access
  data: T[];               // The strongly typed parsed JSON array
  errors: ErrorMap[];      // Collection of bitmap row errors
  
  // Triggers
  importFile: (file: File) =&gt; Promise&lt;void&gt;; // Call this from onChange/onDrop
  reset: () =&gt; void;       // Clears all state and data to start over

  // Reactive State Flags
  isIdle: boolean;
  isParsing: boolean;
  isComplete: boolean;
  isError: boolean;
&#125;</code></pre>

  <div class="mt-16 flex justify-between">
    <a
      href="/docs/react/advanced"
      class="inline-flex items-center gap-2 px-6 py-3 border border-neutral-300 text-black hover:border-black rounded-md text-sm font-medium no-underline! transition-colors"
      style="text-decoration:none;"
    >
      <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"
        ><path
          stroke-linecap="round"
          stroke-linejoin="round"
          stroke-width="2"
          d="M10 19l-7-7m0 0l7-7m-7 7h18"></path>
      </svg>
      Previous
    </a>
  </div>
</Layout>
